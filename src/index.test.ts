it('Should pass', () => {
  expect(1).toBe(1);
});

// it('Should return message that server is on', async (done) => {
//   const response = await request.get('/');

//   expect(response.status).toBe(200);
//   expect(response.body.message).toBe('O servidor ta on!');
//   done();
// });
